# @dtc/backend

Medusa 2 commerce API for this monorepo. Exposes the store API on `:9000` and the admin dashboard at `:9000/app`. Runs migrations and seeds demo data on first boot via `docker-entrypoint.sh`.

For setup, environment variables, deployment, and troubleshooting see the [root README](../../README.md).
